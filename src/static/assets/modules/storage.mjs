import { addLeaderboardEntry,addSession,normalizeBookmarksPayload,normalizeLeaderboardPayload,normalizeProgressImport,normalizeSessionsPayload,STORAGE_LIMITS,toggleBookmark } from "/assets/lib/progress-core.mjs";import { safeJsonParse } from "/assets/lib/text-core.mjs";
export const STORAGE_KEYS=Object.freeze({theme:"codeclarity:theme:v1",preferences:"codeclarity:preferences:v1",sessions:"codeclarity:sessions:v1",leaderboard:"codeclarity:leaderboard:v1",bookmarks:"codeclarity:bookmarks:v1"});
function read(k){try{return localStorage.getItem(k)}catch{return null}}function write(k,v){try{localStorage.setItem(k,JSON.stringify(v));return true}catch{return false}}export function remove(k){try{localStorage.removeItem(k);return true}catch{return false}}
export const getSessions=()=>normalizeSessionsPayload(read(STORAGE_KEYS.sessions));export function saveSessions(v){const n=normalizeSessionsPayload(v);write(STORAGE_KEYS.sessions,n);return n}export function storeSession(s){const n=addSession(getSessions(),s);saveSessions(n);setPreferences({latestSessionId:s.id});return n}
export const getLeaderboard=()=>normalizeLeaderboardPayload(read(STORAGE_KEYS.leaderboard));export function saveLeaderboard(v){const n=normalizeLeaderboardPayload(v);write(STORAGE_KEYS.leaderboard,n);return n}export function storeLeaderboardEntry(s,i){const n=addLeaderboardEntry(getLeaderboard(),s,i);saveLeaderboard(n);return n}
export const getBookmarks=()=>normalizeBookmarksPayload(read(STORAGE_KEYS.bookmarks));export function saveBookmarks(v){const n=normalizeBookmarksPayload(v);write(STORAGE_KEYS.bookmarks,n);return n}export function updateBookmark(id){const n=toggleBookmark(getBookmarks(),id);saveBookmarks(n);return n}
export function getPreferences(){const p=safeJsonParse(read(STORAGE_KEYS.preferences),null);return !p||p.version!==1?{version:1,latestSessionId:""}:{version:1,latestSessionId:typeof p.latestSessionId==="string"?p.latestSessionId.slice(0,100):""}}export function setPreferences(u){const n={...getPreferences(),...u,version:1};write(STORAGE_KEYS.preferences,n);return n}
export function importProgress(v){const n=normalizeProgressImport(v);if(!n)return null;saveSessions(n.sessions);saveLeaderboard(n.leaderboard);saveBookmarks(n.bookmarks);return n}export function clearProgress(){remove(STORAGE_KEYS.sessions);remove(STORAGE_KEYS.bookmarks);setPreferences({latestSessionId:""})}export function clearLeaderboard(){remove(STORAGE_KEYS.leaderboard)}export{STORAGE_LIMITS};
