import { announce,clear,el,qs,show } from "/assets/modules/dom.mjs";import { clearLeaderboard,getLeaderboard } from "/assets/modules/storage.mjs";const root=qs("[data-leaderboard-root]");if(root){const body=qs("[data-leaderboard-body]",root),empty=qs("[data-leaderboard-empty]",root),button=qs("[data-leaderboard-clear]",root);function render(){const items=getLeaderboard().items;clear(body);items.forEach((e,i)=>{const r=el("tr");r.append(el("th",{text:`#${i+1}`,attrs:{scope:"row"}}),el("td",{text:e.initials}),el("td",{text:e.score}),el("td",{text:`${e.accuracy}%`}),el("td",{text:`${e.remainingSeconds}s`}));body.append(r)});show(empty,!items.length);button.disabled=!items.length}button.addEventListener("click",()=>{if(confirm("Clear all local sprint scores?")){clearLeaderboard();announce("Leaderboard cleared.");render()}});render()}
